language: 'java'
sudo: required
jdk:
  - 'oraclejdk8'

branches:
  only:
  - 'master'

install:
  - 'bundle install'
  - 'pip install --user html5validator'

before_script:
 -  'chmod +x ./assets/dev/build'

script:
  - 'html5validator --root _site/ --ignore "⚡" "custom-element" "amp-boilerplate" "amp-custom" "on" '
  - './assets/dev/build'

env:
  global:
  - 'NOKOGIRI_USE_SYSTEM_LIBRARIES=true'

before_install:
  - 'sudo update-ca-certificates'
